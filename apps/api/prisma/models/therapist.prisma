model Therapist {
  therapistId                        String                            @id @unique
  userId                             String                            @unique
  mobile                             String
  province                           String
  timezone                           String                            @default("UTC")
  status                             TherapistApplicationStatus        @default(PENDING)
  submissionDate                     DateTime                          @default(now())
  processingDate                     DateTime                          @default(now())
  processedByAdminId                 String?
  providerType                       ProviderType
  professionalLicenseType            String
  prcLicenseNumber                   String
  expirationDateOfLicense            DateTime
  practiceStartDate                  DateTime
  licenseVerified                    Boolean                           @default(false)
  licenseVerifiedAt                  DateTime?
  licenseVerifiedBy                  String?
  certifications                     Json?
  certificateUrls                    String[]                          @default([])
  certificateNames                   String[]                          @default([])
  licenseUrls                        String[]                          @default([])
  licenseNames                       String[]                          @default([])
  documentUrls                       String[]                          @default([])
  documentNames                      String[]                          @default([])
  yearsOfExperience                  Int?
  educationBackground                String?
  specialCertifications              String[]
  practiceLocation                   String?
  acceptsInsurance                   Boolean                           @default(false)
  acceptedInsuranceTypes             String[]
  areasOfExpertise                   String[]
  otherAreaOfExpertise               String?
  therapeuticApproachesUsedList      String[]
  languagesOffered                   String[]
  providedOnlineTherapyBefore        Boolean
  comfortableUsingVideoConferencing  Boolean
  preferredSessionLength             Int[]
  privateConfidentialSpace           String?
  compliesWithDataPrivacyAct         Boolean
  complaintsOrDisciplinaryActions    String?
  willingToAbideByPlatformGuidelines Boolean
  expertise                          String[]
  approaches                         String[]
  languages                          String[]
  illnessSpecializations             String[]
  acceptTypes                        String[]
  treatmentSuccessRates              Json
  sessionLength                      String
  hourlyRate                         Decimal                           @db.Decimal(10, 2)
  preferOnlineOrOffline              String?
  willingToCaterOutsideCebu          Boolean                           @default(false)
  preferredPayrollAccount            String?
  createdAt                          DateTime                          @default(now())
  updatedAt                          DateTime                          @updatedAt
  assignedClients                    ClientTherapist[]
  meetings                           Meeting[]                         @relation("TherapistMeetings")
  paymentsAsTherapist                Payment[]
  reviews                            Review[]
  processedByAdmin                   Admin?                            @relation(fields: [processedByAdminId], references: [userId])
  therapistAvailabilities            TherapistAvailability[]
  worksheets                         Worksheet[]
  GroupSessionTherapistInvitation    GroupSessionTherapistInvitation[]
  performance                        TherapistPerformance?
  packages                           Therapist_Packages[]

  @@index([therapistId])
  @@index([status])
  @@index([timezone])
  @@index([status, createdAt])
  @@index([status, hourlyRate])
  @@index([province])
  @@index([yearsOfExperience])
  @@index([hourlyRate])
  @@index([createdAt])
  @@index([province, status])
}

enum TherapistApplicationStatus {
  PENDING
  APPROVED
  REJECTED
}

enum ProviderType {
  LICENSED_PSYCHOLOGIST
  LICENSED_GUIDANCE_COUNSELOR
}
