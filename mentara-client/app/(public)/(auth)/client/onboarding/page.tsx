"use client";

import { ClientOnboarding } from "@/components/auth/client";

export default function ClientOnboardingPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-white to-slate-100">
      <ClientOnboarding />
    </div>
  );
}